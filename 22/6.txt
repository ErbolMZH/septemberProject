<!-- Поле 1: Клиент подтверждал транзакцию -->
<div class="col-md-12 form-group mb-3">
  <label style="color: #fff;">Клиент подтверждал транзакцию *</label>
  <select 
    v-model="form.is_client_confirmed" 
    class="form-control" 
    @change="onClientConfirmationChange"
    required
  >
    <option :value="false">Не подтверждал</option>
    <option :value="true">Подтверждал</option>
  </select>
</div>

<!-- Поле 2: Способ подтверждения (Отображается только если Подтверждал) -->
<div class="col-md-12 form-group mb-3" v-if="form.is_client_confirmed">
  <label style="color: #fff;">Способ подтверждения *</label>
  <select 
    v-model="form.confirmation_method" 
    class="form-control" 
    required
  >
    <option value="" disabled selected hidden>Каким способом клиент подтвердил операцию</option>
    <option 
      v-for="item in confirmationMethods" 
      :key="item.code" 
      :value="item.code"
    >
      {{ item.name_ru || item.code }}
    </option>
  </select>
</div>

<!-- Поле 3: Подтверждение легитимности (Отображается только если Подтверждал) -->
<div class="col-md-12 form-group mb-3" v-if="form.is_client_confirmed">
  <label style="color: #fff;">Подтверждение клиентом легитимности операции *</label>
  <select 
    v-model="form.legitimacy_confirmation" 
    class="form-control" 
    required
  >
    <option value="" disabled selected hidden>Выберите значение</option>
    <option 
      v-for="opt in legitimacyOptions" 
      :key="opt.code" 
      :value="opt.code"
    >
      {{ opt.name }}
    </option>
  </select>
</div>
